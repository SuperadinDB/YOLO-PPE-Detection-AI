tracker_type: bytetrack

track_high_thresh: 0.25
track_low_thresh: 0.10
new_track_thresh: 0.30

# 100 frames ≈ 4 segundos a 25 FPS
track_buffer: 100

# Más permisivo para conservar identidad
match_thresh: 0.85

fuse_score: True